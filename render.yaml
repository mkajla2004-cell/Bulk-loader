services:
  - type: web
    name: bulkloader-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 -m Bot"
    plan: free
    envVars:
      - key: APP_ID
      - key: API_HASH
      - key: BOT_TOKEN
      - key: OWNER_ID
        sync: false

        
