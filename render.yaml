services:
  - type: web
    name: bulkloader-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 -m Bot"
    plan: free
    envVars:
      - key: APP_ID, API_HASH, BOT_TOKEN,OWNER_ID
        sync: false

        
